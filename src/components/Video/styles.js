import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  videoClass: {
    backgroundColor: '#121212',
    paddingTop: 10,
    minHeight: 320,
    maxHeight: 320,
    borderRadius: 10,
  },
  videoTitle: {
    color: 'white',
  },
});
