import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  favorites: {
    textAlign: 'center',
  },
  light: {
    color: 'black',
  },
  dark: {
    color: 'white',
  },
});
